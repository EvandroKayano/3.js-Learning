    Make clones of a model by clicking in highlighted spot on a grided plane

After replacing the yellow ball from clicking on the highlighted plane, the stag model will be texture wise mixed. That's
because the .clone() method DOESN'T WORK on models, only in meshes

import * as SkeletonUtils

EVERY MODEL HAS ITS MIXER